# XORNG Automation Server Configuration
# Copy this file to .env and fill in your values

# Required: GitHub Personal Access Token with repo and admin:org scopes
GITHUB_TOKEN=ghp_your_github_token_here

# GitHub Organization name (default: XORNG)
GH_ORG=XORNG

# Optional: Webhook secret for signature verification
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=your_webhook_secret_here

# Optional: Public URL where this server is accessible
# Used for registering webhooks automatically
WEBHOOK_URL=https://xorng.yourdomain.com/webhook/github

# Server configuration
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# Optional: Redis URL for caching and task queue persistence
# REDIS_URL=redis://localhost:6379

# ===========================================
# AI Provider Configuration (OpenRouter)
# ===========================================
# OpenRouter provides unified access to multiple AI models
# Get your API key at: https://openrouter.ai/keys
# 
# For GitHub Actions deployment, configure these as repository secrets:
# - OPENROUTER_API_KEY: Your OpenRouter API key
# - OPENROUTER_MODEL: Model to use (optional, defaults to anthropic/claude-sonnet-4)
#
# TEMPORARY SOLUTION: This configuration uses GitHub Secrets to pass the API key
# to the automation server. A more permanent solution will use organization-level
# secrets or a dedicated secrets management system.

# OpenRouter API Key (required for AI-powered features)
OPENROUTER_API_KEY=sk-or-your_openrouter_key_here

# OpenRouter Model ID (optional, see https://openrouter.ai/models)
# Popular options:
#   - anthropic/claude-sonnet-4 (default, good balance)
#   - anthropic/claude-3.5-sonnet (fast responses)
#   - openai/gpt-4o (alternative)
#   - google/gemini-2.0-flash-001 (fast, cost-effective)
OPENROUTER_MODEL=anthropic/claude-sonnet-4

# ===========================================
# Legacy AI Provider Configuration (deprecated)
# ===========================================
# Direct provider keys are deprecated in favor of OpenRouter
# ANTHROPIC_API_KEY=sk-ant-your_key
# OPENAI_API_KEY=sk-your_key
